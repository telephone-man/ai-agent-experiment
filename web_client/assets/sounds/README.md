# UI Sound Asset Notes

The MP3 files in this directory are local browser UI tones used by
`web_client/call/call.js`.

Their original source and license are not currently known. Keep them treated as
demo placeholders, and replace them with clearly licensed tones before using
this client outside the local proof of concept or redistributing it as a
product.
